dependencies:
  cache_directories:
    - "vendor/bundle"
  override:
    - script/bootstrap

test:
  override:
    - script/test
    - script/build
    - script/feature

deployment:
  deploy:
    branch: master
    commands:
      - ./script/deploy
